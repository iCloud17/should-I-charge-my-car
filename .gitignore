# macOS
.DS_Store

# Node (only used by optional seed scripts)
node_modules/
npm-debug.log*
.env
.env.local

# Editor
.vscode/*
!.vscode/extensions.json

# Logs
*.log

# Local browser-testing artifacts (screenshots, Playwright output)
.playwright-mcp/
/*.png
# ...except the social-preview image, which is a shipped asset.
!/og-image.png

# Seed script cache (large bulk CSV from fueleconomy.gov)
scripts/.vehicles.cache.csv

# Local design notes (not part of the shipped app)
DESIGN.md

# Vendored AI design skill (ui-ux-pro-max) + Copilot prompt wrapper; local tooling, not shipped.
# Ignore everything under .github/ EXCEPT the Pages deploy workflow, which must
# be tracked so it runs on GitHub.
.github/*
!.github/workflows/
.github/workflows/*
!.github/workflows/deploy.yml